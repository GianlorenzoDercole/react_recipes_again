
export default function Ingredient({ ingredient }) {

    return(
        // show the text for each item in the ingredients array
        <div>
            <h3>{ingredient.text}</h3>
        </div>
    )
}
